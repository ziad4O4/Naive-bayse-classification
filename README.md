# Naive-bayse-classification
its a mini project for one method of classification (naive bayse) using python

when entring the columnms name of the dataset whether the target column or the normal columns, know that u must enter them exactly like the dataset because its case sensetive.
